# ValueCell 生产环境部署完整指南

> **作者**: Claude AI Assistant
> **更新日期**: 2025-11-17
> **适用版本**: ValueCell 0.1.0+

---

## 📖 目录

1. [部署架构设计](#部署架构设计)
2. [Docker 容器化部署](#docker-容器化部署)
3. [Kubernetes 集群部署](#kubernetes-集群部署)
4. [监控和告警](#监控和告警)
5. [安全加固](#安全加固)
6. [性能优化](#性能优化)
7. [备份和灾备](#备份和灾备)
8. [运维最佳实践](#运维最佳实践)

---

## 部署架构设计

### 推荐架构

```
┌──────────────────────────────────────────────────────────────┐
│                         负载均衡器                             │
│                     (Nginx / Traefik)                        │
└────────────────┬─────────────────────────────────────────────┘
                 │
        ┌────────┴────────┐
        │                 │
┌───────▼──────┐  ┌──────▼────────┐
│   前端服务    │  │   API 服务     │
│   (React)    │  │  (FastAPI)    │
│   Port 1420  │  │   Port 8000   │
└──────────────┘  └───────┬────────┘
                          │
        ┌─────────────────┼─────────────────┐
        │                 │                 │
┌───────▼──────┐  ┌──────▼────────┐  ┌────▼──────┐
│ 研究智能体     │  │ 交易智能体     │  │ 新闻智能体 │
│  Port 8001   │  │  Port 8002    │  │ Port 8003 │
└──────┬───────┘  └───────┬───────┘  └────┬──────┘
       │                  │                │
       └──────────────────┼────────────────┘
                          │
        ┌─────────────────┼─────────────────┐
        │                 │                 │
┌───────▼──────┐  ┌──────▼────────┐  ┌────▼──────┐
│   SQLite     │  │   LanceDB     │  │  Redis    │
│  (对话存储)   │  │  (向量数据库)  │  │ (缓存)    │
└──────────────┘  └───────────────┘  └───────────┘
```

### 部署模式选择

#### 1. **单机部署**（适合个人/小团队）
- **优点**：简单、成本低
- **缺点**：无高可用性
- **适用场景**：开发、测试、个人使用

#### 2. **容器化部署**（推荐）
- **优点**：隔离性好、易于扩展、版本管理方便
- **缺点**：需要学习 Docker
- **适用场景**：生产环境、团队协作

#### 3. **集群部署**（企业级）
- **优点**：高可用、可扩展、负载均衡
- **缺点**：复杂度高、成本高
- **适用场景**：大规模用户、关键业务

---

## Docker 容器化部署

### 1. Dockerfile 编写

#### 后端 Dockerfile

```dockerfile
# 文件: python/Dockerfile
FROM python:3.12-slim

# 设置工作目录
WORKDIR /app

# 安装 uv（快速包管理器）
RUN pip install uv

# 复制依赖文件
COPY pyproject.toml uv.lock ./

# 安装依赖
RUN uv sync --frozen

# 复制代码
COPY . .

# 暴露端口
EXPOSE 8000

# 健康检查
HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
  CMD curl -f http://localhost:8000/health || exit 1

# 启动命令
CMD ["uv", "run", "valuecell/server/main.py"]
```

#### 智能体 Dockerfile

```dockerfile
# 文件: python/Dockerfile.agent
FROM python:3.12-slim

WORKDIR /app

# 安装依赖
RUN pip install uv
COPY pyproject.toml uv.lock ./
RUN uv sync --frozen

# 复制代码
COPY . .

# 参数化智能体名称
ARG AGENT_NAME
ENV AGENT_NAME=${AGENT_NAME}

EXPOSE 8001

HEALTHCHECK --interval=30s --timeout=10s \
  CMD python -c "import requests; requests.get('http://localhost:8001/health')"

CMD uv run -m valuecell.agents.${AGENT_NAME}
```

#### 前端 Dockerfile

```dockerfile
# 文件: frontend/Dockerfile
FROM node:18-slim AS builder

WORKDIR /app

# 安装 bun
RUN npm install -g bun

# 复制依赖文件
COPY package.json bun.lock ./

# 安装依赖
RUN bun install --frozen-lockfile

# 复制代码
COPY . .

# 构建
RUN bun run build

# 生产镜像
FROM nginx:alpine

# 复制构建产物
COPY --from=builder /app/build/client /usr/share/nginx/html

# 复制 nginx 配置
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

### 2. Docker Compose 配置

```yaml
# 文件: docker-compose.yml
version: '3.8'

services:
  # ========== 数据库服务 ==========
  redis:
    image: redis:7-alpine
    container_name: valuecell-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes
    networks:
      - valuecell-network
    restart: unless-stopped

  # ========== 后端 API 服务 ==========
  api:
    build:
      context: ./python
      dockerfile: Dockerfile
    container_name: valuecell-api
    ports:
      - "8000:8000"
    environment:
      - API_DEBUG=false
      - API_PORT=8000
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
      - sqlite-data:/app/
      - lancedb-data:/app/lancedb
    depends_on:
      - redis
    networks:
      - valuecell-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # ========== 智能体服务 ==========
  research-agent:
    build:
      context: ./python
      dockerfile: Dockerfile.agent
      args:
        AGENT_NAME: research_agent
    container_name: valuecell-research-agent
    ports:
      - "8001:8001"
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - SEC_EMAIL=${SEC_EMAIL}
    volumes:
      - ./logs:/app/logs
      - agent-cache:/app/.cache
    networks:
      - valuecell-network
    restart: unless-stopped

  auto-trading-agent:
    build:
      context: ./python
      dockerfile: Dockerfile.agent
      args:
        AGENT_NAME: auto_trading_agent
    container_name: valuecell-trading-agent
    ports:
      - "8002:8002"
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - OKX_NETWORK=${OKX_NETWORK:-paper}
      - OKX_API_KEY=${OKX_API_KEY}
      - OKX_API_SECRET=${OKX_API_SECRET}
      - OKX_API_PASSPHRASE=${OKX_API_PASSPHRASE}
      - OKX_ALLOW_LIVE_TRADING=${OKX_ALLOW_LIVE_TRADING:-false}
    volumes:
      - ./logs:/app/logs
    networks:
      - valuecell-network
    restart: unless-stopped

  news-agent:
    build:
      context: ./python
      dockerfile: Dockerfile.agent
      args:
        AGENT_NAME: news_agent
    container_name: valuecell-news-agent
    ports:
      - "8003:8003"
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - FINNHUB_API_KEY=${FINNHUB_API_KEY}
    volumes:
      - ./logs:/app/logs
    networks:
      - valuecell-network
    restart: unless-stopped

  # ========== 前端服务 ==========
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: valuecell-frontend
    ports:
      - "1420:80"
    depends_on:
      - api
    networks:
      - valuecell-network
    restart: unless-stopped

  # ========== Nginx 反向代理 ==========
  nginx:
    image: nginx:alpine
    container_name: valuecell-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
    depends_on:
      - frontend
      - api
    networks:
      - valuecell-network
    restart: unless-stopped

volumes:
  redis-data:
  sqlite-data:
  lancedb-data:
  agent-cache:

networks:
  valuecell-network:
    driver: bridge
```

### 3. Nginx 配置

```nginx
# 文件: nginx/nginx.conf
upstream api_backend {
    server api:8000;
}

upstream frontend_backend {
    server frontend:80;
}

server {
    listen 80;
    server_name valuecell.example.com;

    # 重定向到 HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name valuecell.example.com;

    # SSL 证书
    ssl_certificate /etc/nginx/ssl/cert.pem;
    ssl_certificate_key /etc/nginx/ssl/key.pem;

    # SSL 配置
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    # 安全头
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # API 路由
    location /api {
        proxy_pass http://api_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;

        # WebSocket 支持
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # 前端路由
    location / {
        proxy_pass http://frontend_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 静态文件缓存
    location ~* \.(jpg|jpeg|png|gif|ico|css|js)$ {
        proxy_pass http://frontend_backend;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

### 4. 部署步骤

```bash
# 1. 克隆代码
git clone https://github.com/ValueCell-ai/valuecell.git
cd valuecell

# 2. 配置环境变量
cp .env.example .env
# 编辑 .env 文件，填入必要的 API 密钥

# 3. 构建镜像
docker-compose build

# 4. 启动服务
docker-compose up -d

# 5. 查看日志
docker-compose logs -f

# 6. 检查服务状态
docker-compose ps

# 7. 访问服务
# 前端: http://localhost
# API: http://localhost/api
# API 文档: http://localhost/api/docs

# 8. 停止服务
docker-compose down

# 9. 清理数据（谨慎）
docker-compose down -v
```

---

## Kubernetes 集群部署

### 1. Kubernetes 资源定义

#### ConfigMap（配置管理）

```yaml
# k8s/configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: valuecell-config
  namespace: valuecell
data:
  API_DEBUG: "false"
  API_PORT: "8000"
  API_HOST: "0.0.0.0"
  REDIS_HOST: "redis-service"
  REDIS_PORT: "6379"
  LANG: "en-US"
  TIMEZONE: "America/New_York"
```

#### Secret（敏感信息）

```yaml
# k8s/secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: valuecell-secrets
  namespace: valuecell
type: Opaque
stringData:
  OPENROUTER_API_KEY: "sk-or-v1-xxx"
  OKX_API_KEY: "xxx"
  OKX_API_SECRET: "xxx"
  OKX_API_PASSPHRASE: "xxx"
  FINNHUB_API_KEY: "xxx"
```

#### Deployment（API 服务）

```yaml
# k8s/api-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: valuecell-api
  namespace: valuecell
spec:
  replicas: 3
  selector:
    matchLabels:
      app: valuecell-api
  template:
    metadata:
      labels:
        app: valuecell-api
    spec:
      containers:
      - name: api
        image: valuecell/api:latest
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: valuecell-config
        - secretRef:
            name: valuecell-secrets
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "2Gi"
            cpu: "2000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
        volumeMounts:
        - name: logs
          mountPath: /app/logs
        - name: data
          mountPath: /app/data
      volumes:
      - name: logs
        persistentVolumeClaim:
          claimName: valuecell-logs-pvc
      - name: data
        persistentVolumeClaim:
          claimName: valuecell-data-pvc
```

#### Service（服务暴露）

```yaml
# k8s/api-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: valuecell-api-service
  namespace: valuecell
spec:
  selector:
    app: valuecell-api
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
  type: ClusterIP
```

#### Ingress（外部访问）

```yaml
# k8s/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: valuecell-ingress
  namespace: valuecell
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  tls:
  - hosts:
    - valuecell.example.com
    secretName: valuecell-tls
  rules:
  - host: valuecell.example.com
    http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: valuecell-api-service
            port:
              number: 8000
      - path: /
        pathType: Prefix
        backend:
          service:
            name: valuecell-frontend-service
            port:
              number: 80
```

#### HorizontalPodAutoscaler（自动扩缩容）

```yaml
# k8s/hpa.yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: valuecell-api-hpa
  namespace: valuecell
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: valuecell-api
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
```

### 2. 部署到 Kubernetes

```bash
# 1. 创建命名空间
kubectl create namespace valuecell

# 2. 应用配置
kubectl apply -f k8s/configmap.yaml
kubectl apply -f k8s/secret.yaml

# 3. 部署服务
kubectl apply -f k8s/redis-deployment.yaml
kubectl apply -f k8s/api-deployment.yaml
kubectl apply -f k8s/agent-deployments.yaml
kubectl apply -f k8s/frontend-deployment.yaml

# 4. 创建服务
kubectl apply -f k8s/services.yaml

# 5. 配置 Ingress
kubectl apply -f k8s/ingress.yaml

# 6. 启用自动扩缩容
kubectl apply -f k8s/hpa.yaml

# 7. 查看部署状态
kubectl get pods -n valuecell
kubectl get svc -n valuecell
kubectl get ing -n valuecell

# 8. 查看日志
kubectl logs -f deployment/valuecell-api -n valuecell

# 9. 执行命令
kubectl exec -it <pod-name> -n valuecell -- /bin/bash
```

---

## 监控和告警

### 1. Prometheus + Grafana

#### 指标收集器配置

```yaml
# prometheus/prometheus.yml
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'valuecell-api'
    static_configs:
      - targets: ['api:8000']
    metrics_path: '/metrics'

  - job_name: 'valuecell-agents'
    static_configs:
      - targets:
        - 'research-agent:8001'
        - 'auto-trading-agent:8002'
        - 'news-agent:8003'

  - job_name: 'redis'
    static_configs:
      - targets: ['redis:6379']
```

#### 添加监控指标到代码

```python
# python/valuecell/server/metrics.py
from prometheus_client import Counter, Histogram, Gauge
import time

# 请求计数器
request_count = Counter(
    'valuecell_requests_total',
    'Total request count',
    ['method', 'endpoint', 'status']
)

# 请求延迟
request_latency = Histogram(
    'valuecell_request_duration_seconds',
    'Request latency',
    ['method', 'endpoint']
)

# 活跃智能体数
active_agents = Gauge(
    'valuecell_active_agents',
    'Number of active agents',
    ['agent_type']
)

# 交易指标
trades_total = Counter(
    'valuecell_trades_total',
    'Total trades executed',
    ['exchange', 'symbol', 'action']
)

trade_value = Histogram(
    'valuecell_trade_value_usd',
    'Trade value in USD'
)

# 使用示例
@app.middleware("http")
async def add_metrics(request, call_next):
    start_time = time.time()

    response = await call_next(request)

    duration = time.time() - start_time
    request_latency.labels(
        method=request.method,
        endpoint=request.url.path
    ).observe(duration)

    request_count.labels(
        method=request.method,
        endpoint=request.url.path,
        status=response.status_code
    ).inc()

    return response
```

#### Grafana 仪表板配置

```json
{
  "dashboard": {
    "title": "ValueCell 监控仪表板",
    "panels": [
      {
        "title": "API 请求速率",
        "targets": [
          {
            "expr": "rate(valuecell_requests_total[5m])"
          }
        ]
      },
      {
        "title": "P95 延迟",
        "targets": [
          {
            "expr": "histogram_quantile(0.95, rate(valuecell_request_duration_seconds_bucket[5m]))"
          }
        ]
      },
      {
        "title": "活跃智能体数",
        "targets": [
          {
            "expr": "valuecell_active_agents"
          }
        ]
      },
      {
        "title": "交易数量",
        "targets": [
          {
            "expr": "rate(valuecell_trades_total[1h])"
          }
        ]
      }
    ]
  }
}
```

### 2. 日志聚合（ELK Stack）

#### Filebeat 配置

```yaml
# filebeat/filebeat.yml
filebeat.inputs:
- type: log
  enabled: true
  paths:
    - /app/logs/**/*.log
  fields:
    service: valuecell
  multiline:
    pattern: '^\d{4}-\d{2}-\d{2}'
    negate: true
    match: after

output.logstash:
  hosts: ["logstash:5044"]

processors:
  - add_host_metadata: ~
  - add_docker_metadata: ~
```

### 3. 告警规则

```yaml
# prometheus/alerts.yml
groups:
- name: valuecell_alerts
  rules:
  # API 错误率告警
  - alert: HighErrorRate
    expr: |
      rate(valuecell_requests_total{status=~"5.."}[5m]) /
      rate(valuecell_requests_total[5m]) > 0.05
    for: 5m
    labels:
      severity: critical
    annotations:
      summary: "API 错误率过高"
      description: "错误率 {{ $value | humanizePercentage }}"

  # 延迟告警
  - alert: HighLatency
    expr: |
      histogram_quantile(0.95,
        rate(valuecell_request_duration_seconds_bucket[5m])
      ) > 1.0
    for: 5m
    labels:
      severity: warning
    annotations:
      summary: "API 延迟过高"
      description: "P95 延迟 {{ $value }}s"

  # 智能体离线告警
  - alert: AgentDown
    expr: up{job="valuecell-agents"} == 0
    for: 1m
    labels:
      severity: critical
    annotations:
      summary: "智能体服务离线"
      description: "{{ $labels.instance }} 已离线"

  # 交易异常告警
  - alert: TradingAnomalies
    expr: |
      rate(valuecell_trades_total[1h]) >
      rate(valuecell_trades_total[1h] offset 24h) * 3
    for: 10m
    labels:
      severity: warning
    annotations:
      summary: "交易频率异常"
      description: "交易频率比24小时前高3倍"
```

---

## 安全加固

### 1. 环境变量管理

```bash
# 使用密钥管理服务
# AWS Secrets Manager
aws secretsmanager get-secret-value \
  --secret-id valuecell/prod/api-keys \
  --query SecretString \
  --output text | jq -r .OPENROUTER_API_KEY

# HashiCorp Vault
vault kv get -field=OPENROUTER_API_KEY secret/valuecell/prod
```

### 2. API 访问控制

```python
# python/valuecell/server/security.py
from fastapi import Security, HTTPException, status
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials
import jwt

security = HTTPBearer()

def verify_token(credentials: HTTPAuthorizationCredentials = Security(security)):
    """验证 JWT Token"""
    try:
        token = credentials.credentials
        payload = jwt.decode(
            token,
            os.getenv("JWT_SECRET"),
            algorithms=["HS256"]
        )
        return payload
    except jwt.ExpiredSignatureError:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="Token 已过期"
        )
    except jwt.InvalidTokenError:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="无效 Token"
        )

# 使用
@app.get("/api/protected")
async def protected_route(payload = Depends(verify_token)):
    return {"message": "已认证", "user": payload["user_id"]}
```

### 3. 速率限制

```python
from fastapi_limiter import FastAPILimiter
from fastapi_limiter.depends import RateLimiter
import redis.asyncio as redis

@app.on_event("startup")
async def startup():
    redis_client = redis.from_url("redis://localhost")
    await FastAPILimiter.init(redis_client)

@app.get("/api/data", dependencies=[Depends(RateLimiter(times=10, seconds=60))])
async def get_data():
    """每分钟最多10次请求"""
    return {"data": "..."}
```

---

## 性能优化

### 1. 数据库优化

```python
# SQLite 性能优化
import sqlite3

conn = sqlite3.connect("valuecell.db")
cursor = conn.cursor()

# 启用 WAL 模式（并发性能提升）
cursor.execute("PRAGMA journal_mode=WAL")

# 增加缓存大小
cursor.execute("PRAGMA cache_size=10000")  # 10MB

# 优化 checkpoint
cursor.execute("PRAGMA wal_autocheckpoint=1000")
```

### 2. Redis 缓存策略

```python
import redis
from functools import wraps
import json

redis_client = redis.Redis(host='localhost', port=6379, db=0)

def cache_result(ttl=3600):
    """缓存装饰器"""
    def decorator(func):
        @wraps(func)
        async def wrapper(*args, **kwargs):
            # 生成缓存键
            cache_key = f"{func.__name__}:{hash((args, frozenset(kwargs.items())))}"

            # 尝试从缓存获取
            cached = redis_client.get(cache_key)
            if cached:
                return json.loads(cached)

            # 执行函数
            result = await func(*args, **kwargs)

            # 存入缓存
            redis_client.setex(
                cache_key,
                ttl,
                json.dumps(result)
            )

            return result
        return wrapper
    return decorator

# 使用
@cache_result(ttl=300)  # 缓存5分钟
async def get_market_data(symbol):
    # 昂贵的数据获取操作
    data = await fetch_from_api(symbol)
    return data
```

---

## 备份和灾备

### 自动化备份脚本

```bash
#!/bin/bash
# backup.sh

BACKUP_DIR="/backups/valuecell/$(date +%Y%m%d)"
mkdir -p "$BACKUP_DIR"

# 1. 备份 SQLite
sqlite3 valuecell.db ".backup '$BACKUP_DIR/valuecell.db'"

# 2. 备份 LanceDB
tar -czf "$BACKUP_DIR/lancedb.tar.gz" lancedb/

# 3. 备份配置文件
cp .env "$BACKUP_DIR/.env"

# 4. 上传到 S3
aws s3 sync "$BACKUP_DIR" "s3://valuecell-backups/$(date +%Y%m%d)/"

# 5. 清理旧备份（保留30天）
find /backups/valuecell/ -mtime +30 -delete

echo "备份完成: $BACKUP_DIR"
```

---

继续创建剩余文档...
